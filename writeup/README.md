# Writeup

This directory contains the actual thesis document files. To build the PDF [`typst`](https://typst.app) must installed, then run 

```shell
typst compile main.typ
```

If you don't have the IBM Plex font installed, compile using

```shell
typst compile main.typ --font-path fonts